# Dummy Sample CC App

This sample app runs inference of a simple tflite model using LiteRT with
hardware acceleration (NPU or CPU). The model adds two input tensors and returns
the result.

## Run sample

To build and run the sample, connect a device and run:
```shell
litert/samples/google/cc/dummy_sample/run_sample_cc_app.sh
```
